# Production build defaults (committed; VITE_* are public). The Clerk publishable key is NOT
# here — set VITE_CLERK_PUBLISHABLE_KEY (pk_live_...) in the Cloudflare Pages build environment
# for the jotmodel-app project so it isn't committed and can be rotated independently.
VITE_WORKER_HOST=api.jotmodel.com
